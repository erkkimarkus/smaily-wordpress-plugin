logs/
vendor/
node_modules/
dist/
coverage/
.env
smaily-connect.zip
smaily-connect-*.zip
*.zip
languages/*.po~

# Local PHPCS / PHPStan / PHPUnit override files (the .dist versions are
# committed; developers can drop a local copy here that the tools pick up
# first).
phpcs.xml
phpstan.neon
phpunit.xml
.phpunit.result.cache

# Test coverage output.
tmp/
.phpcs-cache
.claude/
